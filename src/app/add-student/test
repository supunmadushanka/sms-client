<form [formGroup]="addStudent" (ngSubmit)="studentSubmit()">

    <div class="form-row">
        <div class="form-group col-md-6">
            <label for="firstName">First Name</label>
            <input [class.is-invalid]="firstName.touched && firstName.invalid" formControlName="firstName" type="text" class="form-control form-control-lg" id="firstName" required placeholder="First name">
        </div>

        <div class="form-group col-md-6">
            <label for="secondName">Last Name</label>
            <input [class.is-invalid]="secondName.touched && secondName.invalid" formControlName="secondName" type="text" class="form-control form-control-lg" id="secondName" required placeholder="Last name">
        </div>

        <div *ngIf="(firstName.touched && firstName.invalid) || secondName.invalid && secondName.touched">
            <small class="text-danger">First name and last name reuired and must contain more than 3 charactors<br></small>
        </div>
    </div><br>

    <div class="form-group">
        <label for="Address">Address</label>
        <input [class.is-invalid]="addStudent.get('Address').invalid && addStudent.get('Address').touched" formControlName="Address" type="text" class="form-control form-control-lg" id="Address" required placeholder="1234 Main St">
        <small [class.d-none]="addStudent.get('Address').valid || addStudent.get('Address').untouched" class="text-danger">Permenent address required</small>
    </div><br>

    <div class="form-group">
        <label for="DOB">Date of birth</label>
        <input [class.is-invalid]="addStudent.get('DOB').invalid && addStudent.get('DOB').touched" formControlName="DOB" type="date" class="form-control form-control-lg" id="DOB">
        <small [class.d-none]="addStudent.get('DOB').valid || addStudent.get('DOB').untouched" class="text-danger">Date of birth required</small>
    </div><br>

    <div class="form-check form-check-inline">
        <input formControlName="Gender" class="form-check-input" type="radio" name="Gender" id="maleplayer" value="male" required>
        <label class="form-check-label" for="maleplayer">Male</label>
    </div>&nbsp;
    <div class="form-check form-check-inline">
        <input formControlName="Gender" class="form-check-input" type="radio" name="Gender" id="femaleplayer" value="female" required>
        <label class="form-check-label" for="femaleplayer">female</label>
    </div>
    <div *ngIf="addStudent.get('Gender').errors?.required" class="text-danger"><small>required</small></div>
    <br>

    <div class="form-group">
        <label for="Family">Family</label>
        <select [class.is-invalid]="addStudent.get('Family').invalid && addStudent.get('Family').touched" formControlName="Family" class="form-control" id="exampleFormControlSelect1">
            <option class="option" *ngFor="let fm of Families" [value]=fm.strutureId>{{fm.strutureName}}</option>
        </select>
        <small [class.d-none]="addStudent.get('Family').valid || addStudent.get('Family').untouched" class="text-danger">Required</small>
    </div><br>

    <button type="reset" class="btn btn btn-outline-dark">Reset</button>&nbsp;&nbsp;&nbsp;
    <button [disabled]="addStudent.invalid" type="submit" class="btn btn-primary">Next</button>

</form>